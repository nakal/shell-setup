source ~/.mutt/local.muttrc
source ~/.mutt/colors.muttrc

# Edit
set edit_hdrs
set signature="~/.mutt/signature"
set fast_reply
set include

# View
set sort=threads
set sort_aux=date-sent
set beep=no
set beep_new=yes
set forward_quote
set indent_str="> "
ignore *
unignore From: Subject: Date: To: CC: Bcc: X-Mailer:
unhdr_order *
hdr_order From: Subject: Date: To: CC: Bcc: X-Mailer:

# Cache
set message_cachedir="~/.cache/mutt"
set header_cache="~/.cache/mutt"

# sidebar
set sidebar_width=25
set sidebar_visible=yes
set sidebar_delim='|'
#set sidebar_sort=yes
#set sidebar_format='%B%N%S'
#set sidebar_folderindent=yes
#color sidebar_new yellow default
bind index "'" sidebar-prev
bind index "\\" sidebar-next
bind index \c? sidebar-open
bind pager "'" sidebar-prev
bind pager "\\" sidebar-next
bind pager \c? sidebar-open

# IMAP
unset imap_passive
set imap_keepalive=300
set mail_check=5
#set imap_check_subscribed
set record="=Sent"
set postponed="=Drafts"
set timeout=10
set net_inc=5
set copy
unset mark_old

# addressbook
#set query_command="lbdbq %s"
#set query_command="abook --mutt-query '%s'"
#macro index \ca !abook\n
#macro pager A |'abook --add-email'\n

# PGP
set pgp_replyencrypt
set pgp_replysign
set pgp_replysignencrypted
set pgp_show_unusable=no
